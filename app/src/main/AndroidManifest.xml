<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swiftpot.android.tariffplanner">
<uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!--Optionally, the android.permission.WAKE_LOCK permission -->
    <!--if the application needs to keep the processor from sleeping when a message is received.-->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <permission android:name="com.swiftpot.android.tariffplanner.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.swiftpot.android.tariffplanner.permission.C2D_MESSAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_tv"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".Application">
        android:theme="@style/AppTheme">
        <!--GCM ish-->
        <!--GCM ish-->
        <receiver
            android:name="com.google.android.gms.gcm.GcmReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <!--If you want to support pre-4.4 KitKat devices, -->
            <!--add the following action to the intent filter declaration for the receiver: -->
            <!--<action android:name="com.google.android.c2dm.intent.REGISTRATION" />-->
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <category android:name="com.example.gcm" />
            </intent-filter>
        </receiver>
        <!--A declaration of GcmListenerService, which enables various aspects of handling messages such as -->
        <!--detecting different downstream message types, determining upstream send status, -->
        <!--and automatically displaying simple notifications on the app’s behalf-->
        <service
            android:name=".services.RegistrationIntentService"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>

        <!--A service that extends InstanceIDListenerService, to handle the
        creation, rotation, and updating of registration tokens.-->
        <service
            android:name=".services.MyInstanceIDListenerService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.android.gms.iid.InstanceID" />
            </intent-filter>
        </service>
        <!--GCM ish-->
        <!--GCM ish-->
        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
